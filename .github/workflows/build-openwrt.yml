name: Build_OpenWrt_24.10_Intel

on:
  workflow_dispatch:

env:
  REPO_URL: https://github.com/openwrt/openwrt
  REPO_BRANCH: openwrt-24.10
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: 1. 检出仓库 & 清理磁盘
      uses: actions/checkout@v4

    - name: 空间深度清理
      uses: jlumbroso/free-disk-space@main
      with:
        tool-cache: true
        android: true
        dotnet: true
        haskell: true

    - name: 2. 初始化环境 & 升级工具链
      run: |
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install $(curl -fsSL https://is.gd/depends_ubuntu_2204)
        # 针对 24.10 需要更高版本的 Go
        sudo add-apt-repository ppa:longsleep/golang-backports -y
        sudo apt update
        sudo apt install golang-go -y
        sudo timedatectl set-timezone "$TZ"

    - name: 3. 拉取源码与 Feeds
      run: |
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        cd openwrt
        # 添加核心插件源
        echo "src-git kenzo https://github.com/kenzok8/openwrt-packages" >> feeds.conf.default
        echo "src-git small https://github.com/kenzok8/small-package" >> feeds.conf.default
        
        ./scripts/feeds update -a
        
        # 【关键】强制剔除报错包 geoview
        rm -rf feeds/small/geoview
        rm -rf feeds/kenzo/geoview
        
        ./scripts/feeds install -a

    - name: 4. 固件功能配置 (.config)
      run: |
        cd openwrt
        # 架构设置: Intel x86_64
        echo "CONFIG_TARGET_x86=y" >> .config
        echo "CONFIG_TARGET_x86_64=y" >> .config
        echo "CONFIG_TARGET_x86_64_DEVICE_generic=y" >> .config
        
        # 中文支持与界面
        echo "CONFIG_LUCI_LANG_zh_Hans=y" >> .config
        echo "CONFIG_PACKAGE_luci=y" >> .config
        
        # 网卡驱动 (E1000, E1000E, USB 网卡)
        echo "CONFIG_PACKAGE_kmod-e1000=y" >> .config
        echo "CONFIG_PACKAGE_kmod-e1000e=y" >> .config
        echo "CONFIG_PACKAGE_kmod-usb-net=y" >> .config
        echo "CONFIG_PACKAGE_kmod-usb-net-asix=y" >> .config
        echo "CONFIG_PACKAGE_kmod-usb-net-rtl8152=y" >> .config
        
        # 插件集成 (Passwall, SmartDNS, ADG, DDNS, 多拨)
        echo "CONFIG_PACKAGE_luci-app-passwall=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-smartdns=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-adguardhome=y" >> .config
        echo "# CONFIG_PACKAGE_luci-app-adguardhome_INCLUDE_binary is not set" >> .config
        echo "CONFIG_PACKAGE_luci-app-ddns=y" >> .config
        echo "CONFIG_PACKAGE_ddns-scripts-cloudflare=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-syncdial=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-mwan3=y" >> .config
        
        # IPv6 支持
        echo "CONFIG_PACKAGE_ipv6helper=y" >> .config
        
        make defconfig

    - name: 5. 预下载 (防止 Code 1 下载中断)
      run: |
        cd openwrt
        make download -j8
        find dl -size -1024c -exec rm -f {} \;

    - name: 6. 编译 (分步执行)
      run: |
        cd openwrt
        # 首次多线程，失败则进入单线程模式捕获日志
        make -j$(nproc) || make -j1 V=s

    - name: 7. 整理固件并上传
      uses: actions/upload-artifact@v4
      with:
        name: OpenWrt_24.10_Intel_x86_64
        path: openwrt/bin/targets/x86/64/
